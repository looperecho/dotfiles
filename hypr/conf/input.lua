-- Input Hardware Settings

hl.config({
    input = {
        -- Keyboard
        -- ──────────────
        kb_layout  = "us",
        kb_variant = "",
        kb_model   = "",
        kb_options = "",
        kb_rules   = "",

        -- Mouse
        -- ──────────────
        follow_mouse    = 2,
        natural_scroll  = true,
        sensitivity     = 0,  -- -1.0 to 1.0

        -- Touchpad
        -- ──────────────
        touchpad = {
            natural_scroll = true,
        },
    }
})
