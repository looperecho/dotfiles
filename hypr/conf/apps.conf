# App Declerations

# ──────────────
# APPS
# ──────────────
$terminal = wezterm
$browser = firefox
$fileManager = nautilus

# ──────────────
# Launcher
$execMenu = ulauncher-toggle
$menu = gapplication launch io.ulauncher.Ulauncher

# ──────────────
# Spotify
$execMedia = spotify-launcher
$media = Spotify
